# 源码依据

## 基线

| 项目 | 值 |
|---|---|
| 扫描日期 | 2026-09-15 |
| Git commit | `4405059afe5cab5e93f0fd3fd66fc17841747941` |
| Commit 时间 | 2026-08-16T16:56:38+08:00 |
| 包版本 | 0.1.0 |
| Python 要求 | >=3.10 |
| 测试结果 | `190 passed in 3.39s` |

## 扫描范围

本次逐类检查了：

- `configs/demands.yaml`、`configs/emotions.yaml`、`configs/personality.yaml`
- `marsdog_core/*.py` 全部核心类型、状态、需求、情绪、性格、时间和 ONE1000 协议实现
- `marsdog_ros2/*.py` 全部节点和适配器
- `marsdog_ros2/common/*.py` JSON、QoS、参数和时间公共工具
- `marsdog_ros2/launch/*.launch.py` 三个启动文件
- `tests/*.py` 全部 20 个测试文件
- `setup.py`、`setup.cfg`、`pyproject.toml`、`package.xml`
- `tools/export_emotion_event_catalog.py`，只用于确认当前工具行为，没有采用其已有输出作为文档事实

为避免历史内容影响结论，没有读取本目录之外旧文档的正文。`build/`、`install/`、`log/` 和 `__pycache__/` 是生成物或运行记录，不作为最新协议事实源。

## 关键事实位置

| 事实 | 当前源码 |
|---|---|
| 枚举、别名、取值范围 | `marsdog_core/types.py` |
| 初始运行态 | `marsdog_core/state.py` |
| 需求阈值、自然增长、动作映射 | `configs/demands.yaml` |
| 50 个情绪事件、阈值、衰减 | `configs/emotions.yaml` |
| 性格预设 | `configs/personality.yaml` |
| 行为结果结构校验和 ID 去重 | `marsdog_core/behavior_result.py` |
| 需求状态/信号格式 | `marsdog_core/need_system.py`、`marsdog_core/demand_api.py` |
| 情绪状态/信号格式 | `marsdog_core/emotion_system.py`、`marsdog_core/emotion_api.py` |
| 性格状态和公式 | `marsdog_core/personality_api.py` |
| 虚拟时间和调度器 | `marsdog_core/time_controller.py` |
| 接收/发布话题、QoS、频率 | `marsdog_ros2/*_node.py` |
| ONE1000 帧和 TLV | `marsdog_core/one1000_protocol.py` |
| ONE1000 JSON 输出 | `marsdog_ros2/one1000_tactile_node.py` |

## 验证方式

执行：

```bash
cd /home/bird/Marsdog
python3 -m pytest -q
```

结果：190 项全部通过。测试覆盖配置契约、需求边界、情绪边界与去重、行为结果、时间倍率和调度、凌晨场景、适配器、包结构以及 ONE1000 协议。

当前环境没有运行中的 ROS2 图和 ONE1000 硬件，因此本次没有做真实 DDS/QoS 互通或串口实机测试；相关文档描述来自节点创建参数、协议实现及对应自动化测试。

## 文档与源码对应关系

- 接口事实：以 ROS2 节点的 `create_publisher`、`create_subscription` 和消息构造方法为准。
- 字段合法性：以适配器、Normalize 方法和具体业务方法为准，不以示例里出现过为准。
- 数值规则：以运行时实际读取的配置键和公式为准。
- “未接入”判断：配置或公共 API 存在，但从 ROS2 订阅入口到该逻辑没有调用链。
- “可优化”判断：不改变当前协议事实，只指出一致性、可观测性、扩展性或可靠性问题。

[project]
name = "marsdog-need-emotion"
version = "0.1.0"
description = "Marsdog internal need, emotion, personality and virtual time ROS2 nodes."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
